/**
 * Tailwind CSS v4 — Configuration is done in CSS, not here.
 *
 * This project uses @tailwindcss/vite (v4), which reads configuration
 * from `src/index.css` via the `@theme {}` directive.
 *
 * ✅ To add/modify colors, fonts, spacing, etc.:
 *    → Edit the `@theme {}` block in `src/index.css`
 *
 * This file is intentionally left minimal and is NOT read by Tailwind v4.
 */