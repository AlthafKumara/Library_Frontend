/**
 * DashboardPage — Main landing page after the splash screen.
 *
 * Replace the placeholder content with real dashboard widgets,
 * stats, and data once the UI components are ready.
 */
export default function DashboardPage() {
  return (
    <div className="w-full min-h-screen flex flex-col justify-center items-center bg-neutral-200 gap-4">
      <h1 className="text-4xl font-bold text-neutral-900">📚 Library Dashboard</h1>
      <p className="text-neutral-500 text-lg">Welcome! Your dashboard is ready.</p>
    </div>
  );
}
